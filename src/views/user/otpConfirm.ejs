<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quên mật khẩu</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .container {
            max-width: 500px;
            margin-top: 50px;
        }
        .error-message {
            color: red;
        }
    </style>
</head>
<body>

<div class="container">
    <h2 class="text-center">Xác nhận OTP quên mật khẩu</h2>

    <% if (session && session.error) { %>
        <div class="alert alert-danger">
            <%= session.error %>
        </div>
        <% delete session.error; %> <!-- Xóa thông báo sau khi hiển thị -->
    <% } %>

    <form action="/confirm-otp" method="POST" style="margin-top: 20px;">
        <div class="form-group">
            <label for="otp">Nhập mã OTP đã được gửi đến email:</label>
            <input type="text" class="form-control" id="otp" name="otp" required="">
        </div>
        
        <button type="submit" class="btn btn-primary btn-block">Xác nhận mã OTP</button>
    </form>
</div>

</body>
</html>